server:
    port: 8070
    
eureka:
    instance:
        hostname: "localhost"
    client:
        fetchRegistry: false
        registeredWithEureka: false
        serviceUrl: http://${eureka.instance.hostname}:${server.port}/eureka/